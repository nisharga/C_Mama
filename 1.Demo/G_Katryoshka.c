#include <stdio.h> 
int main()
{ 
    int n, m, k, eyesRemain, answer = 0;
    scanf("%d %d %d", &n, &m, &k);
    if(n > k){
        eyesRemain = n - k;
        answer = m;
        k = k - 
    }
    if(
     
    printf("%d", n);
    return 0;
}
Two eyes and one body. 
One eye, one mouth, and one body.

k every time need one...
one mouth is optional..
but eye one must need..

90 24 89
66 0  65 = 24 times
33 0     = 33 